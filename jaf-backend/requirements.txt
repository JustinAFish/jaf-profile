# Lambda-optimized requirements - reduced package size
mangum==0.17.0

# Core FastAPI dependencies (lightweight)
fastapi==0.115.5
pydantic==2.11.7
pydantic-settings==2.7.0
python-dotenv==1.0.1

# Essential web handling
python-multipart==0.0.9

# Per-IP rate limiting for the public chat endpoint
slowapi==0.1.10

# ASGI server (Railway / start.py; not used on Lambda)
uvicorn[standard]==0.32.1

# LangChain stack (langchain-pinecone 0.2.13+ uses pinecone>=6, not pinecone-client)
langchain-core==0.3.84
langchain==0.3.27
langchain-pinecone==0.2.13
langchain-openai==0.3.35
langchain-community==0.3.27

# OpenAI (lightweight; pinned for langchain-openai)
openai==1.104.2

# Vector database (asyncio extra required by langchain-pinecone)
pinecone[asyncio]==7.3.0

# Cohere (reranking via Rerank v2 API)
cohere>=5.0.0

# Essential dependencies only
requests==2.32.5

# Document loading (minimal set)
pypdf==3.17.4

# LangSmith (langchain-core 0.3.78+ requires >=0.3.45)
langsmith==0.3.45
